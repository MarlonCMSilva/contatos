import styled from 'styled-components'
import { Campo } from '../../styles'

export const Container = styled.div`
  margin-bottom: 20px;
  ${Campo} {
    width: 200px;
    margin: 8px 8px;
    align-items: left;
  }
`
